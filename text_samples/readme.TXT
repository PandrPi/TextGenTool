The folder where this file is placed on stores the text files.
These text files can be used to create a little word dictionary.
However, words number of created dictionary can be used only for testing, because in some cases the size of the created dictionary cannot fit all the user's needs.